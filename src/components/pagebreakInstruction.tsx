export default async function pagebreakInstruction() {
  return (
    <>
      Caution... You should only have one page break at a time, if you have more than one the page
      break will be chosen that is at the top of the table
    </>
  )
}
